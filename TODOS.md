# TODOS

## 设计 / QA

- **[P2] 朗读开关上线后视觉与无障碍 QA**  
  实现顶栏「朗读」开关（`PracticeView` 与静态 `index.html` 若同步）后，跑一次实机 `/design-review`：小屏顶栏是否折行、触控目标 ≥44px、`footer-stats` 的 `aria-live` 与系统朗读是否叠读。  
  **Depends on:** 朗读功能合并后主分支可访问的练习页。

- **[P2] 键声 + 朗读同时开启时的听感与无障碍 QA**  
  顶栏「键声」与「朗读」可同时开：实机听一遍「朗读成字 + 键声」是否嘈杂、是否需互斥开关说明；`prefers-reduced-motion` 等是否映射到静音（产品决策）。  
  **Depends on:** 键声与朗读功能已在练习页可用。

## 已定案（不入表）

- **D1C：**默认朗读**关**；首次说明文案在对应 PR 内与开关一并定稿（不写进本文件）。
